endpoints:
  web:
    exposure:
      include: "*"

zuul:
  routes:
#     user: /user/**
#     order: /order/**
    myProduct:
      path: /myProduct2/**
      serviceId: product
#      允许传递HttpServletRequest所有东西
      sensitiveHeaders:
      
    order:
      path: /order2/**
      serviceId: order
#      允许传递HttpServletRequest所有东西
      sensitiveHeaders:
      
    myUser:
      path: /myUser/**
      serviceId: user
      #      允许传递HttpServletRequest所有东西
      sensitiveHeaders:
#   简洁写法
#    product: /myProduct/**
#    以下接口不会对外暴露
#  ignored-patterns:
#    - /**/product/listForOrder
    
